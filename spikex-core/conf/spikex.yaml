# ------------------------------------------------------------------------------
# Spike.x main configuration
# ------------------------------------------------------------------------------

modules:
    - { id: io.spikex~spikex-filter~0.9.1 }
    - { id: io.spikex~spikex-metrics~0.9.1, config: { update-interval: 15000 }}
